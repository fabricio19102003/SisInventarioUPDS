# ─────────────────────────────────────────────────────────────
# App Web Principal — @upds/web
# Copiar este archivo a .env y completar los valores
# ─────────────────────────────────────────────────────────────

# ── Base de Datos ─────────────────────────────────────────────
# Cadena de conexión PostgreSQL.
# Formato: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://postgres:password@localhost:5432/inventario_upds

# ── Autenticación (NextAuth v5) ───────────────────────────────
# Clave secreta para firmar y verificar tokens JWT.
# Generar con: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here-change-in-production

# URL base de la aplicación (usada por NextAuth para callbacks y redirects).
# En producción usar la URL pública (ej: https://inventario.upds.edu.bo)
AUTH_URL=http://localhost:3000
